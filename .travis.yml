
language: python

matrix:
    include:
        python: 2.7
        dist: xenial
        sudo: true
python:
"
  - "2.7"

# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - pytest